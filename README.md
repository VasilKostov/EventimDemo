# EventimDemo

This is is a demo version of Eventim (website for ordering tickets for concerts). This is a console project with Postrgre Database, so in order to run it, you should have the suitable driver for the database and the tables created.
